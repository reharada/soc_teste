package screenplay.abilities;


import java.io.IOException;
import java.util.concurrent.TimeUnit;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;



public class UsesChrome {
	
	//protected static WebDriver driver;
	
	/*
	 * public BuscaPage(WebDriver driver) { this.driver = driver; }
	 * 
	 * String url;
	 * 
	 * public UsesChrome(String url) throws IOException { this.url = url;
	 * initializeFAP(url); }
	 * 
	 * public void initializeFAP(String url) throws IOException {
	 * System.setProperty("webdriver.chrome.driver",
	 * ".\\src\\test\\resources\\driver\\chrome\\85\\chromedriver.exe");
	 * ChromeDriver driver = new ChromeDriver();
	 * driver.manage().window().maximize();
	 * driver.manage().timeouts().implicitlyWait(50000, TimeUnit.MILLISECONDS);
	 * driver.get(url);
	 * 
	 * }
	 */

}
